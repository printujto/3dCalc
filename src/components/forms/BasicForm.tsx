import React from 'react'

const BasicForm = () => {
    return (
        <form
            className='w-96 flex flex-col gap-2 mt-2'
            // onSubmit={(e) => submitForm(e)}
        ></form>
    )
}

export default BasicForm
